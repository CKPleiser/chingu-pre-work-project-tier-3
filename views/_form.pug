h1  Digital Journal | Chingu Project Tier 3
form#submit_note(action="." method="POST")
  .form-group
    label(for="title") Title:
    input.form-control(
      type="text"
      id="title"
      name="title"
    )

  .form-group
    label(for="note") Body:
    textarea.form-control(
      id="note"
      rows="3"
    )

  button.btn.btn-primary Submit